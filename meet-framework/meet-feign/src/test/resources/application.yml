spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 60000
            readTimeout: 60000
          demoFeignClient:
            connectTimeout: 3000
            readTimeout: 3000
