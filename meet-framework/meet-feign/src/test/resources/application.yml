spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 60000
            readTimeout: 60000
          demoFeignClient:
            connectTimeout: 3000
            readTimeout: 3000


log:
  feign:
    # 不开启feign日志
    #    style: none
    #    style: aop
    style: interceptor